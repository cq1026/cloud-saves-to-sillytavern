# SillyTavern 数据目录（宿主机路径）
ST_DATA_PATH=/var/sillytavern/data

# 备份仓库路径（容器内路径，通常不需要修改）
BACKUP_REPO_PATH=/backup

# GitHub 仓库地址（使用 HTTPS 格式）
GITHUB_REMOTE_URL=https://github.com/username/sillytavern-backup.git

# GitHub Personal Access Token（用于认证）
# 获取方式：GitHub Settings -> Developer settings -> Personal access tokens -> Tokens (classic)
# 权限需要：repo (完整权限)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 备份时间（24小时制，格式 HH:MM）
BACKUP_TIME=03:00

# 是否启用自动备份（true/false）
AUTO_BACKUP_ENABLED=true

# 日志文件最大大小（MB）
MAX_LOG_SIZE_MB=10
